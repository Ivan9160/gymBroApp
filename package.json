{
  "dependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.3.1",
    "mkcert": "^3.2.0",
    "react-redux": "^9.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.2.0"
  },
  "scripts": {
    "start:all": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\" \"npm run nginx:start\"",
    "nginx:start": "node ./scripts/start-nginx.js"
  }
}
